[build]
  bin = "./tmp/main"
  cmd = "go build -o ./tmp/main ./cmd/server/."
  include_ext = ["go", "templ"]
  exclude_regex = ["_test.go", "_templ.go"]
  exclude_dir = ["tmp", "vendor", "playground"]
  pre_cmd = ["templ generate"]
  delay = 1000
